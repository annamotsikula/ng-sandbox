import { ProductCategoryDirective } from './product-category.directive';

describe('ProductCategoryDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductCategoryDirective();
    expect(directive).toBeTruthy();
  });
});
